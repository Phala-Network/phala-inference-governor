[package]
name = "pig_governor_core"
version = "0.2.0"
edition = "2021"
license = "Apache-2.0"
description = "Actual Decode feedback and bounded scheduling advice for PIG"

[lib]
crate-type = ["cdylib", "rlib"]

[profile.release]
panic = "unwind"

[workspace]
